# Backups directory (brief 09, O2)

Nightly compressed Postgres dumps land here (host-mounted into the `backup`
service). This directory is git-ignored except for this README.

See ../docs/BACKUP_RESTORE.md for restore and off-device copy instructions.
